export interface PlayTableProps {
}

/** Full play-table scene: opponent row, spell stage, hand, and variant controls. Takes no props. */
export function PlayTable(props?: PlayTableProps): JSX.Element;
